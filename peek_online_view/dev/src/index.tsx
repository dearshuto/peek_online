import React from "react";
import ReactDOM from "react-dom/client";
import PeekOnline from "peek_online_view";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <PeekOnline />
  </React.StrictMode>,
);
